/**
 * auth.js
 * Written by: Alex Kasemir
 */
const initialState = {
  token: ``,
};

/**
 * Description of the auth reducer
 */
const auth = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default auth;
