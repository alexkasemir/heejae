/**
 * byId.js
 * Written by: Alex Kasemir
 */

/**
 * Description of the byId reducer
 */
const byId = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default byId;
